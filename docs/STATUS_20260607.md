# EvenV11 형식화 — 현재 상태 정돈 (2026-06-07)

"잠시 멈춤" 시점의 단일 orientation 문서. 빌드 green, `V28Hard` live umbrella
green, Main honest sorry 6개, **spine 실제 native_decide 0개**.

## 1. 주정리와 6개 홀

`EvenV11.Main.evenModulusToriAllDimensions : EvenModulusToriAllDimensionsGoal`
(= 모든 짝수 m≥4, d≥2의 `D_d(m)` 분해)는 무조건적으로 서술됨. 남은 수학은 6개
`assume_*` 홀:

| 홀 | Lean obligation (Main) | 논문 | 경로/상태 | 난이도 |
|---|---|---|---|---|
| **H1** | `assume_d3EvenRootFlat : FinalD3EvenRootFlatCertificateFamily := sorry` | §5 terminal A₂ | 아직 reduction 없음(타입만). terminal-cyclicity가 정답(유한 검증됨). m=4 witness `D3EvenM4` 유지 | **하드** |
| **H2** | `assume_lowD5M4RibbonData : Nonempty LowD5M4RibbonData := sorry` | §11 reset | **LIVE physical-row wild-e ribbon**. 단일순환은 완료(Seed). 필요: `PhysicalLayerRows` + singleton-switch RF2 + wild `e` + return realization | **하드(B)** |
| **H3** | `assume_lowD7M4CycleData : Nonempty (RootFlatCycle.RootFlatCycleData 6 4) := sorry` | App A/B | 연결 글루 완료. blob archive. 필요: D7(4) dir+RF1/RF2/RF3 | **하드** |
| **H4** | `assume_lowD7M6CycleData : Nonempty (RootFlatCycle.RootFlatCycleData 6 6) := sorry` | App A/B | H3와 동형. blob archive | **하드** |
| **H5** | `assume_oddHighModulus := sorry` | §6 coforest + §8 growth | 아직 reduction 없음 | **하드(파라메트릭)** |
| **H6** | `assume_oddEndpoint := sorry` | §9–10 endpoint | 아직 reduction 없음 | **하드(파라메트릭)** |

> 6개 모두 정직한 sorry. H2/H3/H4는 깨끗한 구조적 핸드오프로 환원됨(연결 끝, 수학만
> 남음). H1/H5/H6은 아직 핸드오프 reduction 미작성(타입 수준 sorry).

## 2. 빌드 / 청정도

- `lake build EvenV11` **green (8393 jobs)**, sorry warning 6개.
- `lake build EvenV11.V28Hard` **green**. Legacy/generated checkpoint files remain under
  `EvenV11/V28Hard/`, but stale archive-dependent modules are no longer imported by the
  live umbrella.
- **Main spine 실제 native_decide 0개.** (디렉터리 토큰 14개 중: `D3EvenM4` 9개=실제
  (umbrella-only, spine 밖); `LowD5M4Structural` 4 + `RootFlatCycleData` 1 = docstring
  "no native_decide" 문구 오탐.)
- `#print axioms evenModulusToriAllDimensions` ⇒ `sorryAx`(+propext/choice/Quot) 예상.

## 3. 깨끗한 인프라 (spine, sorry·native_decide 없음 — 재사용 자산)

- `Shared/RootFlat.lean` — RootFlatSchedule, RF1/RF2/RF3, returnMap, RF criterion.
- `Shared/SwitchCalculus.lean`, `MasterReturn.lean` — cut-splice/ribbon(`flagSplicingCriterion` 등).
- `Shared/Monodromy.lean` — `single_cycle_of_equiv_conj` 등 단일순환 운반.
- `Shared/UnitCarry.lean`, `WordSkew.lean` — Bundle B(unit-carry skew).
- `EvenV11/LowD5M4Seed.lean` — D5(4) 5색 return이 256-cycle (✅, unit-carry).
- `EvenV11/LowD5M4Schedule.lean` — torusEquiv, dir-무관 stepConjugacy.
- `EvenV11/LowD5M4Structural.lean` — H2 ribbon reduction (`e` 파라미터).
- `EvenV11/LowD5M4RibbonInterface.lean` — §11 direct physical row + singleton-switch RF2
  + map-level/pointwise return conjugacy handoff.
- `EvenV11/RootFlatCycleData.lean` — **generic** cycle-data→cert(+D7M4/D7M6 wiring).
- `EvenV11/StandardRootFlatLift.lean` — 표준 lift torusEquiv/rootStep/torusEquiv_step.
- `EvenV11/TerminalA2LowMod.lean` — 터미널 A₂ carriers.

## 4. 검증 도구 / 스크립트

- `scripts/verify_finite_checks.py` (논문) — 통과 ✅.
- `scripts/verify_rootflat_certificates.py` (신규) — RF1/RF2/RF3 검증기 + terminal/
  D5(4)/D2 교차검증, 통과 ✅. **후보 dir을 Lean 증명 전 즉시 검증**(`verify_rootflat(n,m,dir)`).
- `scripts/check_evenv11_progress.sh` — gate(build green + axiom/admit/native_decide 금지 + hole count).

## 5. 주요 문서 (최신)

- `STATUS_20260607.md` (이 문서, 현재 상태).
- `CERTIFICATE_VERIFICATION_20260607.md` — 검산 결과 + dir pseudocode + 구현체크 워크플로.
- `ROUTE_E_AND_DEADCODE_AUDIT_20260607.md` — 데드코드 감사 + archive + Realization 분리 계획.
- `H2_REALIZATION_BLOCKER_20260605.md` — displacement blocker + (A)native_decide/(B)wild-e/(C)ribbon 경로.
- `H2_D5M4_REALIZATION_WORKSHEET_20260604.md` — D5(4) §11 구성 레시피.

## 6. archive (사장된 방법론, retire 완료 — `archive/EvenV11/`)

native_decide blob 3 + Route E 4(+`D3EvenM4RootFlat`) + `Status.lean`. spine 무관,
복원법은 `archive/EvenV11/README.md`. **Route E 중첩 제거 완료.**

2026-06-07 추가 cleanup: archive-dependent v28 staging files도 active `V28Hard`에서
분리했다. `D5M4RibbonRows`, `D5M4H2Skeleton`, `D5M4H2PaperRows`,
`D7FiniteToCycleData`, `D7Checkpoint`는 `archive/EvenV11/V28Hard/` 아래로 이동했고,
active checklist는 `LowD5M4RibbonInterface` + structural D7 input만 참조한다.
또한 `LowD5M4TameObstruction`이 tame `seedRootEquiv` direct-return shortcut을
Lean에서 반례로 차단한다.

## 7. 정돈된 frontier — 다음 결정/작업 후보

1. **[H2 핵심 미해결]** `LowD5M4RibbonInterface.PhysicalRowsSingletonSwitchMapConjInput`
   구성. 즉 논문 §11의 네 physical layer rows를 전사하고, singleton-switch RF2와
   wild `e`에 대한 `returnMap = e ∘ fullReturn ∘ e.symm`을 증명.
2. **[H3/H4]** D7(4)/D7(6) dir 구성(App A/B two-rail relay) → `verify_rootflat`로 검증 →
   `RootFlatCycleData` 입력. (구성 = 하드, 검증 도구는 준비됨.)
3. **[H1]** terminal-A2 cycle-data reduction 작성(H2/H3 패턴) + terminal-cyclicity Lean 증명.
4. **[정리]** archived `LowD5M4Realization`에서 실제 paper row table/finite facts만
   필요한 경우 active `LowD5M4RibbonInterface` target으로 선별 포팅.
5. **[gate]** `D3EvenM4` native_decide 처리(H1 흡수 시) + docstring 토큰 reword ⇒ gate green.

## 8. 한 줄 요약

> 정리의 골격은 무조건적·green이고 spine은 native_decide-free. 저차원 base(H2/H3/H4)는
> 깨끗한 구조적 핸드오프로 환원됨(연결 완료, 수학만 남음). 즉시 결정적 목표는
> **H2 direct physical rows + singleton-switch RF2 + wild-e return conjugacy**.
> H1/H5/H6은 아직 파라메트릭 하드 코어.
