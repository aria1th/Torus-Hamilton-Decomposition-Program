# 형식화 GOAL (2026-06-03 설정)

## 목표 (definition of done)
EvenV11 메인 정리 `evenModulusToriAllDimensions`의 **OPEN HOLES를 6 → 2로** 줄인다.
즉 다음 네 가정을 `sorry` → 실제 증명으로 교체한다:

| task | hole | Main.lean 가정 | payload |
|---|---|---|---|
| #1 | **H2** | `assume_lowD5M4` | D5(4) 유한 base |
| #2 | **H3** | `assume_lowD7M4` | D7(4) two-rail base |
| #3 | **H4** | `assume_lowD7M6` | D7(6) two-rail base |
| #4 | **H6** | `assume_oddEndpoint` | endpoint successor (§9–10) |

검증: 각 교체 후 `lake build EvenV11` green + `bash scripts/check_evenv11_progress.sh`
의 OPEN HOLES 카운트 감소. 최종 `#print axioms evenModulusToriAllDimensions`의
`sorryAx`는 남되, 남은 hole이 정확히 2개(H1·H5)로 좁혀진 상태.

## 범위 밖 (deferred — 의도적 제외)
- **H1** `assume_d3EvenRootFlat` (D3-even, 전 even m): terminal A2 단일순환을
  **파라메트릭**으로 증명해야 함(고정 m decide 불가).
- **H5** `assume_oddHighModulus` (§6 귀납 + §8 four-point growth): guide-phase
  불변량 `G_D`, 차원 귀납 — **최난이도**. 명시적 제외.

메인 정리는 이 둘에 **조건부로 정직하게 유지**된다(둘은 `sorryAx`로 노출).

## 근거
어려운 추상 수학(cut-splice A1, flag A2, 다리 A3, master M, unit-carry B,
det→unit C1, coforest boundary C2, 조립 wrapper)은 **이미 전부 형식화 완료**.
H2·H3·H4·H6은 "검증된 엔진에 구체 표를 끼우는" 작업이라 **새 어려운 수학이 없다**
(노동집약적이나 위험 낮음). H6은 파라메트릭이지만 B(`productExponentSingleCycle`)가
풍부해 단일순환이 거의 거저.

## 실행 순서 (task 의존)
```
#1 H2 (파일럿: 전 파이프라인 검증)
   ├─ #2 H3 (two-rail flag 패턴) ──→ #3 H4 (같은 패턴, 큰 표)
   └─ #4 H6 (endpoint; assembly 배선 공유)
```
H2를 schedule 정의 → 4필드 → certificate 조립 → `assume_*` 교체까지 끝까지 뚫어
틀을 세운 뒤, 나머지를 같은 틀로 반복.

## 공통 조립 경로 (각 hole)
```
returnsSingleCycle ← MasterReturn (flagSplice/cut-splice + unit-carry)
layerBijective     ← P2 partialExchange*_bijective
rowLatin           ← 구조적
stepConjugacy      ← torusEquiv 좌표
        ↓ finalRootFlatTorusCertificate_of_fields
FinalRootFlatTorusCertificate d m → assume_* 교체
```
세부 번들 지도: `docs/FORMALIZATION_BUNDLE_MAP_20260603.md`.
가정 원장: `docs/EVEN_V11_ASSUMPTION_LEDGER_20260603.md`.
